syntax = "proto3";

package server;
option go_package="./movies";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service MoviesServer {
  rpc Ping(Request) returns(Response);
}
